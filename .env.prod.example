POSTGRES_DB=drought
POSTGRES_USER=drought
POSTGRES_PASSWORD=change-me

DATABASE_URL=postgresql+psycopg2://drought:change-me@db:5432/drought

APP_ENV=production
LOG_LEVEL=INFO
CORS_ORIGINS=https://drought.werifum.ir,http://drought.werifum.ir

# Paths used by docker-compose*.yml for /datasets and /spi-cache mounts.
# Replace these with the real host paths on the production machine.
DATASETS_ROOT=F:/Datasets
SPI_CACHE_ROOT=F:/Datasets/DroughtCache/polygon_spi

FRONTEND_PORT=8080

MAP_LIMIT_DEFAULT=2000
MAP_LIMIT_MAX=10000
CACHE_TTL_SHORT_SECONDS=300
CACHE_TTL_MEDIUM_SECONDS=900
CACHE_TTL_LONG_SECONDS=1800
CACHE_TTL_DAILY_SECONDS=86400

FROM python:3.11-slim
WORKDIR /usr/src/app

RUN pip install --no-cache-dir livereload

CMD ["python", "-c", "from livereload import Server; s=Server(); s.watch('.', delay=0.2); s.serve(root='.', host='0.0.0.0', port=8080)"]